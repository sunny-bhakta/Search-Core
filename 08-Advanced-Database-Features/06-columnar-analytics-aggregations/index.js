const searchHits = ['item1', 'item2'];
const columnarSummary = { electronics: 120, books: 45 };

console.log({ searchHits, columnarSummary });
// Output:
// { searchHits: [ 'item1', 'item2' ], columnarSummary: { electronics: 120, books: 45 } }
